class Solution:
    def segregate0and1(self, arr):
        i =0
        count = 0
        while i < len(arr):
            if arr[i]==1:
                arr.pop(i)
                count +=1
            else:
                i+=1
        arr.extend([1]*count)
        return arr
